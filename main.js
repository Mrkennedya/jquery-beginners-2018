$(document).ready(() => {

  $('#crossroads-hide').on('click', () => {
    $('.crossroads').hide();
  });

  $('#crossroads-show').on('click', () => {
    $('.crossroads').show();
  });

  $('#fireBreath-hide').on('click', () => {
    $('.fireBreath').hide();
  });

  $('#fireBreath-show').on('click', () => {
    $('.fireBreath').show();
  });

  $('#meetup-hide').on('click', () => {
    $('.meetup').hide();
  });

  $('#meetup-show').on('click', () => {
    $('.meetup').show();
  });

  $('#spinning-hide').on('click', () => {
    $('.spinning').hide();
  });

  $('#spinning-show').on('click', () => {
    $('.spinning').show();
  });



});
